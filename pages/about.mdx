import { withRouter } from 'next/router'
import BlogPost from '../components/layouts/blog-post'
import Page from '../components/layouts/page'
import Contact from '../components/utils/contact'
import { siteMeta } from '../blog.config'

export const meta = {
  title: 'Hi there — I’m Alison Monteiro',
  summary:
    'A 25yo self-motivated Software Developer based in Fortaleza, Brazil. 🏖 I like creating things for the web.',
}

export default withRouter(({ children, router }) => (
  <Page path={router.pathname} meta={meta}>
    {children}
  </Page>
))

<img width='140' src='/static/profile.png' alt={siteMeta.author} />

A 25yo self-motivated Software Developer based in Fortaleza, Brazil. 🏖

I like creating things for the web.

If you need to get in touch or learn more about what I do, find me on:

<Contact />
